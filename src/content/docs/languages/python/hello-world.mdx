---
title: Python Hello World
description: Deploy a Python hello world to Origin Clouds.
---

import DeployButton from '../../../../components/DeployButton.astro';

Create a file named `main.py` with the following content:

```py
print("Hello, Origin Clouds!")
```

Run it locally:

```bash
python main.py
```

Ready to deploy? Use the Origin Clouds CLI or SDK to push your app.

<DeployButton language="python" />
